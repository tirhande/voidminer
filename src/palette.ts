/**
 * 화면 전체가 공유하는 색.
 *
 * 게임 화면은 하나의 시각 세계로 고정한다 — 라이트 테마 대응을 두지 않는 것은
 * 누락이 아니라 선택이다. 우주는 어둡고, 그 어둠 위에서 발광체만 도드라져야
 * 무엇이 중요한지가 색으로 읽힌다.
 *
 * 팔레트는 세 갈래뿐이다. 차가운 청록이 바탕과 계기를 맡고, 따뜻한 주황이
 * 작동 중인 것을 맡고, 붉은색이 막힌 것을 맡는다. 광물색은 그 위에 얹히는
 * 정보색이라 서로 구분되기만 하면 된다.
 */
export const PALETTE = {
  /** 우주 배경. 완전한 검정이 아니라 아주 옅은 청색을 섞는다 */
  Void: 0x04060d,
  /** 배경 성운의 차가운 쪽 */
  NebulaCool: 0x1b3a5c,
  /** 배경 성운의 따뜻한 쪽 */
  NebulaWarm: 0x4a2a4e,

  /** 함선 선체 */
  Hull: 0x9fb2ce,
  /** 함선 장식과 거점 강조부 */
  Trim: 0x44607a,

  /** 계기와 유도등 */
  Signal: 0x7fe3ff,
  /** 작동 중 — 채굴 레이저와 명중 지점 */
  Active: 0xff9a3c,
  /** 막힘 — 캘 수 없는 광물 */
  Locked: 0xff3b3b,

  /**
   * 광물색.
   *
   * 주광물 넷은 색 계열이 확실히 갈리고, 짝인 부광물은 같은 계열의 밝은 쪽을
   * 쓴다. 소행성을 보면 어느 집안인지가 먼저 읽히고, 주광물인지 부광물인지는
   * 밝기로 갈린다.
   */
  Copper: 0xc8703a,
  Tin: 0xe0c9a8,
  Iron: 0x8794a6,
  Nickel: 0xc8d4e0,
  Titanium: 0x7fd0e8,
  Aluminum: 0xbfeaf6,
  Iridium: 0xd8c2f0,
  Platinum: 0xf0e6ff,

  /** 합금색. 짝인 두 광물 사이에 놓는다 */
  Bronze: 0xb08040,
  NickelSteel: 0xa8b4c4,
  TitaniumAlloy: 0x9fe0f0,
  PlatinumIridium: 0xe6d8ff,
} as const;

/**
 * 후처리 설정.
 *
 * 블룸은 이 화면에서 투자 대비 효과가 가장 큰 항목이다. 발광체가 번지면
 * 저폴리 프리미티브도 우주 공간에 떠 있는 것처럼 보인다.
 */
export const POST_PROCESSING = {
  /** 블룸 세기 */
  BloomStrength: 0.55,
  /** 블룸 번짐 반경 */
  BloomRadius: 0.4,
  /**
   * 이 밝기를 넘는 부분만 번진다.
   *
   * 낮추면 발광체가 아닌 것까지 번져 화면 전체가 뿌옇게 밝아진다. 레이저나
   * 유도등처럼 실제로 빛나는 것만 걸리도록 높게 잡는다.
   */
  BloomThreshold: 0.75,
  /**
   * 톤 매핑 노출.
   *
   * ACES 필믹 톤 매핑은 하이라이트를 눌러 색이 타는 것을 막는 대신 전체를
   * 어둡게 만든다. 노출로 되돌리되, 우주는 어두운 것이 기본이므로 1 을 넘기지
   * 않는다.
   */
  ToneMappingExposure: 0.9,
} as const;

/**
 * 조명 세기.
 *
 * 톤 관련 숫자를 한 파일에 모아두기 위해 여기에 둔다. 화면이 밝거나 어두우면
 * 이 값들과 위의 노출만 만지면 된다.
 *
 * 우주에는 대기 산란이 없어 빛이 닿지 않는 면은 원래 완전히 검다. 환경광과
 * 반구광은 그 면에서 형태가 사라지지 않을 만큼만 받쳐주는 용도이지 화면을
 * 밝히는 용도가 아니다.
 */
export const LIGHTING = {
  /** 위아래로 색이 갈리는 반구광 */
  HemisphereIntensity: 1.0,
  /** 전역 환경광 */
  AmbientIntensity: 0.7,
  /** 먼 항성. 주광이다 */
  KeyIntensity: 2.8,
  /** 반대편에서 윤곽을 잡는 반사광 */
  RimIntensity: 1.2,
} as const;

/**
 * 표면 재질.
 *
 * 금속성이 높으면 반사 하이라이트가 좁고 강해져 점처럼 탄다. 여기에 블룸이
 * 얹히면 눈이 아프다. 환경 맵이 없는 장면이라 금속성을 올려도 얻을 것이 없으
 * 므로 낮게 잡고 거칠기를 올려 하이라이트를 넓게 편다.
 */
export const SURFACE = {
  /** 함선과 거점 선체 */
  HullMetalness: 0.12,
  HullRoughness: 0.78,
  /** 소행성. 바위이므로 거의 반사하지 않는다 */
  RockMetalness: 0.04,
  RockRoughness: 0.95,
} as const;

/**
 * 자체 발광 세기.
 *
 * 블룸이 이 값을 그대로 증폭하므로 조금만 올려도 크게 번진다. 형태가 어둠에
 * 묻히지 않을 만큼만 준다.
 */
export const EMISSION = {
  /** 선체. 그림자면이 완전히 검게 죽지 않을 정도 */
  Hull: 0.22,
  /** 장식부와 유도등 */
  Trim: 0.45,
  /** 소행성 표면 */
  Rock: 0.05,
  /** 광물 파편 */
  Debris: 0.3,
  /** 부광물 파편. 드물게 나오므로 눈에 띄어야 한다 */
  Secondary: 0.55,
} as const;

/**
 * 점광원 세기.
 *
 * 가까이에서 터지는 광원일수록 눈에 직접 들어온다. 블룸과 겹치면 화면이 하얗게
 * 뜨므로 보수적으로 잡는다.
 */
export const POINT_LIGHT = {
  /** 함선 엔진. 추력 최대일 때의 값 */
  Engine: 10,
  /** 채굴 레이저 명중 지점 */
  MiningImpact: 8,
  /** 견인빔 방출구 */
  TractorEmitter: 5,
  /** 거점 유도등 */
  StationBeacon: 16,
} as const;
